# basket.bas

This program required adjustments to the code as part of porting it.

## OCR/Transcription Corrections

**None.**

## Porting

`REM`oved the `RANDOMIZE` call in line 10, and replaced `RND` with `RND(1)` per MS-BASIC requirements.
