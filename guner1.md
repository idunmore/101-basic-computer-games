# guner1.bas

This program required adjustments to the code as part of porting it.

## OCR/Transcription Corrections

**None.**

## Porting

Only `REM`oving the `RANDOMIZE` call on line 120, and switching `RND(0)` calls to `RND(1)` to get different random numbers, was required.
